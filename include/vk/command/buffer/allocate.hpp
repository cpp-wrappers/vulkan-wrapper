#pragma once

#include <core/types/are_exclusively_satsify_predicates.hpp>

#include "../../headers.hpp"

namespace vk {

	namespace internal {
		struct device;
		struct command_buffer;
	}

	template<typename... Args>
	requires(

	)
	internal::command_buffer& allocate() {

	}

}